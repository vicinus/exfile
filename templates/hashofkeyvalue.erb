<%-
if @content.is_a? Hash
  if @additional_parameters.has_key?('merger')
    merger = @additional_parameters['merger']
  else
    merger = ' = '
  end
  @content.sort.each do | key, value | 
    -%><%= "#{key}#{merger}#{value}\n" %><%-
  end
end
-%>
